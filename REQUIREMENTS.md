# 延时统计分析工具 —— 需求规格文档 v1.2

## 目录

1. [项目背景](#1-项目背景)
2. [数据来源](#2-数据来源)
3. [约束条件](#3-约束条件)
4. [成功与失败标准](#4-成功与失败标准)
5. [延时定义](#5-延时定义)
6. [对齐 Key 定义](#6-对齐-key-定义)
7. [统计指标](#7-统计指标)
8. [输出 Markdown 结构](#8-输出-markdown-结构)
9. [文件自动识别规则](#9-文件自动识别规则)
10. [实现方案](#10-实现方案)
11. [命令行接口](#11-命令行接口)
12. [模块划分](#12-模块划分)
13. [边界条件与异常处理](#13-边界条件与异常处理)
14. [数据规模参考](#14-数据规模参考)

---

## 1. 项目背景

基于 Latency Toolkit 采集的 TAP 时间戳中间数据文件，对市场行情数据（快照/逐笔）进行延时统计分析，目标是：

1. **对比**：多路数据之间的相对速度快慢（以指定基准路为参照，其他路减基准路，差值可为负）
2. **分布**：单路数据全天的 TAP→交易所时间绝对延时分布（min/max/avg/stddev/Pxx）

---

## 2. 数据来源

### 2.1 快照（md）

```
格式：${tap_ts_ps},${ticker},${exchange_ts}[,其他字段...]
示例：82313384932802080,563500,20241226153500000
文件名：md_{exchange}_{src_ip}_{dst_ip}_{date}_tap_tmp.txt
```

### 2.2 逐笔（tbt）

```
格式：${tap_ts_ps},${channel},${seq},${ticker},${exchange_ts}[,其他字段...]
示例：82313384932802080,2021,34464517,204001,20241226153500000
文件名：tbt_{exchange}_{src_ip}_{dst_ip}_{date}_tap_tmp.txt
```

### 2.3 文件名示例

```
md_sh_10.0.1.1_10.0.1.2_20241226_tap_tmp.txt
tbt_sz_10.0.1.3_10.0.1.4_20241226_tap_tmp.txt
```

### 2.4 时间戳说明

| 字段 | 类型 | 精度 | 说明 |
|------|------|------|------|
| `tap_ts_ps` | uint64 | ps（皮秒） | TAP 交换机硬件时间戳，约 20 位数字 |
| `exchange_ts` | string | ms（毫秒） | 交易所时间，格式 `YYYYMMDDHHMMSSmmm`，17 位 |

### 2.5 时间戳转换示例

```
tap_ts_ps    = 82313384932802080
exchange_ts  = "20241226153500000"
  → 解析：2024-12-26 15:35:00.000
  → 转换为 ps：(15×3600 + 35×60 + 0) × 1_000_000_000_000 + 0 = 56100000000000000 ps
latency_ns   = (82313384932802080 - 56100000000000000) / 1000 = 26213384932802 ns
```

> **注意**：`exchange_ts` 精度为 ms，转 ps = ms 值 × 1,000,000,000

---

## 3. 约束条件

| 约束 | 说明 |
|------|------|
| 数据规模 | 单文件最大约 10 GB，逐笔单市场可达 1.5 亿行/天 |
| 运行环境 | Linux（OpenEuler 22.03），Python 3，可用 numpy |
| 输出格式 | Markdown 表格，列对齐，数字右对齐，可直接粘贴至报告 |
| 执行方式 | 命令行，参数化输入，支持 stdout 或写文件 |
| 内存限制 | 不可全量 load 大文件，必须流式处理 |

---

## 4. 成功与失败标准

### 成功标准

- 输出完整 Markdown 报告，覆盖所有要求的统计维度和指标
- 延时单位统一 ns，精确到整数
- 内存安全，不 OOM（流式处理大文件）
- 多路文件自动识别（同 exchange + date 下不同 src_ip/dst_ip 组合为多路）
- 表格数字右对齐，标题行清晰

### 失败标准

- 内存 OOM（逐笔文件 10 GB 不能全量 load）
- 延时对比时 key 不对齐（不同路次同一标的时间戳未正确 join）
- 精度丢失（ps→ns 转换截断误差超过 1ns）

---

## 5. 延时定义

### 5.1 对比延时（多路）

同一「对齐 Key」下，非基准路减基准路的 TAP 时间戳差值，单位 ns。差值可为负（表示该路比基准路更快）。

```
diff_ns[route_i] = (tap_ts_ps[route_i] - tap_ts_ps[baseline]) / 1000
```

**规则：**

- 基准路由命令行参数 `--baseline` 指定，格式：`src_ip_dst_ip`（与文件名中一致）
- join 时各路同一 Key 下若有多条记录，取**最小 tap_ts_ps** 参与计算
- 某 Key 下基准路缺失则跳过该 Key，不参与统计
- 基准路自身在对比表中显示 `—`，不输出统计数据

### 5.2 单路绝对延时

单路内，TAP 时间戳减去交易所时间，单位 ns。每路独立统计，不 join。

```
latency_ns = (tap_ts_ps - exchange_ts_ps) / 1000
```

`exchange_ts_ps` 解析步骤：

```
raw      = "20241226153500000"   # 17 位字符串
H        = int(raw[8:10])
M        = int(raw[10:12])
S        = int(raw[12:14])
ms       = int(raw[14:17])
exch_ps  = (H×3600 + M×60 + S) × 1_000_000_000_000 + ms × 1_000_000_000
```

> `exchange_ts` 不含日期部分的绝对时间，`tap_ts_ps` 同理只取当天时间内的值，二者均为当天时间，差值有意义。

---

## 6. 对齐 Key 定义

| 数据类型 | 分析维度 | 对齐 Key | 备注 |
|----------|----------|----------|------|
| 快照（md） | 多路对比 | `(ticker, exchange_ts)` | 同一标的同一交易所时间 |
| 逐笔（tbt） | 多路对比（通道维度） | `(channel, seq)` | 通道内序列号唯一 |
| 快照（md） | 单路延时 | 无需 join，逐行计算 | — |
| 逐笔（tbt） | 单路延时 | 无需 join，逐行计算 | 按 channel 分组 |

> 逐笔对比**只输出通道号维度**（每路一个子表，按 channel 分组）；交易所时间维度不输出。

---

## 7. 统计指标

所有延时值单位 **ns**，全部取整输出：

| 字段 | 说明 |
|------|------|
| `cnt` | 有效配对数（对比）/ 总条数（单路） |
| `min` | 最小值 |
| `max` | 最大值 |
| `avg` | 均值（浮点计算，输出取整） |
| `stddev` | 标准差（浮点计算，输出取整） |
| `p25` | 第 25 百分位 |
| `p50` | 第 50 百分位（中位数） |
| `p75` | 第 75 百分位 |
| `p90` | 第 90 百分位 |
| `p95` | 第 95 百分位 |
| `p99` | 第 99 百分位 |

分位数计算方式：`numpy.percentile`（线性插值），无 numpy 时回退到排序后线性插值。

---

## 8. 输出 Markdown 结构

报告包含四个 Section，顺序如下：

### Section 1：快照——多路对比（按交易所时间对齐）

```markdown
## 快照对比（按交易所时间对齐，基准路：{baseline}）

| 路次                      |  cnt | min(ns) | max(ns) | avg(ns) | stddev | p25(ns) | p50(ns) | p75(ns) | p90(ns) | p95(ns) | p99(ns) |
|:--------------------------|-----:|--------:|--------:|--------:|-------:|--------:|--------:|--------:|--------:|--------:|--------:|
| 10.0.1.1_10.0.1.2（基准） |    — |       — |       — |       — |      — |       — |       — |       — |       — |       — |       — |
| 10.0.1.3_10.0.1.4         |  160 |     -20 |     683 |      96 |     45 |      40 |      80 |     130 |     200 |     300 |     550 |
```

### Section 2：逐笔——多路对比（按通道号对齐）

每个非基准路输出一个子表：

```markdown
## 逐笔对比（按通道号对齐，基准路：{baseline}）

### 10.0.1.3_10.0.1.4 vs 基准路 10.0.1.1_10.0.1.2

| channel |  cnt | min(ns) | max(ns) | avg(ns) | stddev | p25(ns) | p50(ns) | p75(ns) | p90(ns) | p95(ns) | p99(ns) |
|--------:|-----:|--------:|--------:|--------:|-------:|--------:|--------:|--------:|--------:|--------:|--------:|
|    2021 |  ... |     ... |     ... |     ... |    ... |     ... |     ... |     ... |     ... |     ... |     ... |
```

### Section 3：快照——单路绝对延时

```markdown
## 快照单路延时（TAP − 交易所时间）

| 路次              |    cnt | min(ns) | max(ns) |   avg(ns) | stddev | p25(ns) | p50(ns) | p75(ns) | p90(ns) | p95(ns) | p99(ns) |
|:------------------|-------:|--------:|--------:|----------:|-------:|--------:|--------:|--------:|--------:|--------:|--------:|
| 10.0.1.1_10.0.1.2 | 302238 |  120000 |  980000 |    450000 |  85000 |  380000 |  440000 |  510000 |  620000 |  700000 |  850000 |
```

### Section 4：逐笔——单路绝对延时（按通道分组）

每路一个子表：

```markdown
## 逐笔单路延时（TAP − 交易所时间）

### 10.0.1.1_10.0.1.2

| channel |     cnt | min(ns) | max(ns) | avg(ns) | stddev | p25(ns) | p50(ns) | p75(ns) | p90(ns) | p95(ns) | p99(ns) |
|--------:|--------:|--------:|--------:|--------:|-------:|--------:|--------:|--------:|--------:|--------:|--------:|
|    2021 | 1234567 |     ... |     ... |     ... |    ... |     ... |     ... |     ... |     ... |     ... |     ... |
```

---

## 9. 文件自动识别规则

### 9.1 文件名格式

```
md_{exchange}_{src_ip}_{dst_ip}_{date}_tap_tmp.txt
tbt_{exchange}_{src_ip}_{dst_ip}_{date}_tap_tmp.txt
```

### 9.2 Python 解析正则

```python
FILE_PATTERN = re.compile(
    r'^(md|tbt)_(sh|sz)_(.+?)_(.+?)_(\d{8})_tap_tmp\.txt$'
)
# group(1): 类型      md / tbt
# group(2): exchange  sh / sz
# group(3): src_ip    如 10.0.1.1
# group(4): dst_ip    如 10.0.1.2
# group(5): date      如 20241226
```

### 9.3 路次标识

路次统一用 `{src_ip}_{dst_ip}` 格式，与文件名中保持一致。

---

## 10. 实现方案

### 10.1 大文件处理策略（内存安全）

**多路对比流程（流式外排 join）：**

```
Step 1: 各路文件流式读取
        → 提取 (key, tap_ts_ps, route_id)
        → 写入临时文件（每路一个）

Step 2: 各临时文件按 key 外排
        → 调用系统 sort 命令：sort -t'\t' -k1,1
        → 生成有序临时文件

Step 3: 多路归并 join（heapq.merge）
        → 对齐相同 key 的各路记录
        → 取各路最小 tap_ts_ps
        → 计算 diff_ns = (route_tap - baseline_tap) / 1000

Step 4: diff_ns 按 group_key（channel / route）分组
        → 累积到内存列表
        → 统计分位数
```

**单路延时流程（纯流式）：**

```
流式读取 → 逐行计算 latency_ns → 按 group_key 分组累积列表
→ 最终对各组排序计算分位数
```

### 10.2 内存估算与上限控制

| 场景 | 估算 | 策略 |
|------|------|------|
| 快照单路 1 天 | ~302 万条 × 8B ≈ 24 MB | 直接内存列表 |
| 逐笔单路单 channel 1 天 | ~500 万条 × 8B ≈ 40 MB | 直接内存列表 |
| 逐笔单路全部 channel | ~1.5 亿条 × 8B ≈ 1.2 GB | 按 channel 分批处理，处完即释放 |

> 若内存仍不足，可启用 **Reservoir Sampling**（`--reservoir` 参数），p99 误差 <1%。

### 10.3 分位数计算

```python
import numpy as np

def calc_stats(values: list) -> dict:
    arr = np.array(values, dtype=np.int64)
    pcts = np.percentile(arr, [25, 50, 75, 90, 95, 99])
    return {
        'cnt':    int(len(arr)),
        'min':    int(arr.min()),
        'max':    int(arr.max()),
        'avg':    int(round(arr.mean())),
        'stddev': int(round(arr.std())),
        'p25':    int(round(pcts[0])),
        ...
    }
```

---

## 11. 命令行接口

```bash
python3 latency_stat.py \
  --data-dir /path/to/data \
  --date 20241226 \
  --exchange sh \
  --mode all \
  --baseline "10.0.1.1_10.0.1.2" \
  --output report_20241226.md
```

| 参数 | 必填 | 默认 | 说明 |
|------|:----:|------|------|
| `--data-dir` | ✓ | — | 中间数据文件目录 |
| `--date` | ✓ | — | 日期，格式 YYYYMMDD |
| `--exchange` | ✓ | — | sh / sz / all |
| `--baseline` | ✓ | — | 基准路，格式 `src_ip_dst_ip` |
| `--mode` | — | all | md（快照）/ tbt（逐笔）/ all |
| `--output` | — | stdout | 输出 Markdown 文件路径 |
| `--reservoir` | — | 关闭 | 启用 Reservoir Sampling 限制内存 |
| `--reservoir-size` | — | 1000000 | Reservoir 容量（条数） |
| `--tmp-dir` | — | /tmp | 外排临时文件目录 |
| `--outlier-ns` | — | 0（不过滤）| 过滤绝对值超过 N ns 的异常值 |

---

## 12. 模块划分

```
latency_stat.py      主入口，参数解析，流程调度
file_scanner.py      文件自动识别，返回 route 列表
parser.py            行解析：md行 / tbt行 → (key, tap_ts_ps, ...)
sorter.py            外排封装：调用 sort 命令，管理临时文件
joiner.py            多路归并 join：heapq.merge，输出对齐的 diff_ns
stats.py             统计计算：calc_stats()，ReservoirSampler
reporter.py          Markdown 输出：四个 Section，列宽自适应对齐
test_latency_stat.py 单元测试 + 集成测试
```

---

## 13. 边界条件与异常处理

| 情况 | 处理方式 |
|------|---------|
| 某路文件不存在 | 报警告，跳过该路，不影响其他路统计 |
| 某 Key 下基准路缺失 | 跳过该 Key |
| 某路某 channel 数据量为 0 | 跳过该 channel，不输出该行 |
| `tap_ts_ps` 解析溢出 | 丢弃该行，计入 parse_error 计数，stderr 输出汇总 |
| `exchange_ts` 格式异常（非 17 位数字） | 同上 |
| `diff_ns` 异常大（>N ns） | 可选过滤（`--outlier-ns` 参数），默认不过滤 |
| 只有一路数据（无法对比） | Section 1/2 输出提示"仅一路数据，跳过对比"，仍输出 Section 3/4 |

---

## 14. 数据规模参考

| 类型 | 市场 | 日条数 | 单条大小 | 日文件大小 |
|------|------|-------:|--------:|----------:|
| 快照（md） | 沪市（sh） | ~302 万 | 42 B | ~1.18 GB |
| 快照（md） | 深市（sz） | ~150 万 | 42 B | ~601 MB |
| 逐笔（tbt） | 沪市（sh） | ~1.49 亿 | 56 B | ~7.76 GB |
| 逐笔（tbt） | 深市（sz） | ~1.99 亿 | 56 B | ~10.40 GB |

---

*文档版本：v1.2 | 状态：规格完成*
